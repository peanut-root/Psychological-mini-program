# 云评论上线配置

文章评论已支持云开发数据库。开发工具未开启云开发时，会自动回退到本地缓存；正式上线要看到其他用户评论，需要完成下面配置。

## 1. 开启云开发

在微信开发者工具中打开“云开发”，为当前 AppID 创建或选择云环境。项目代码会在 `app.js` 中自动调用 `wx.cloud.init({ traceUser: true })`。

## 2. 创建数据库集合

在云开发数据库中创建两个集合：

- `article_comments`：保存文章评论和回复。
- `article_comment_likes`：保存评论点赞记录。

## 3. 推荐权限

`article_comments`：
- 所有人可读。
- 登录用户可新增。
- 仅创建者可删除自己的评论。

`article_comment_likes`：
- 所有人可读。
- 登录用户可新增自己的点赞。
- 仅创建者可删除自己的点赞。

如果后台权限不允许客户端删除/更新，前端会提示“只能删除自己的评论”或保留本地兜底效果。上线前请用两个微信号分别测试：A 发评论，B 能看到并点赞/回复，A 能删除自己的评论。

## 4. 数据结构

`article_comments` 字段：
- `articleId`：文章 ID。
- `parentId`：回复所属评论 ID；顶层评论为空字符串。
- `content`：评论内容。
- `user`：展示昵称。
- `avatarUrl`：展示头像。
- `userId`：本地生成的用户标识。
- `createdAt`：云数据库服务端时间。

`article_comment_likes` 字段：
- `articleId`：文章 ID。
- `commentId`：被点赞的评论 ID。
- `userId`：本地生成的用户标识。
- `createdAt`：云数据库服务端时间。
